# GeoComp

Hackday Project 

### Getting Started

Requires node, gulp and bower

`brew install node`
`npm install -g gulp`
`npm install -g bower`

### Install packages

`npm install`

`bower install`

### Run

`gulp serve`
